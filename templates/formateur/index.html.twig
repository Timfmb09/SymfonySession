{% extends 'base.html.twig' %}

{% block title %}Hello FormateurController!{% endblock %}

{% block body %}
<<<<<<< HEAD
    <table>
        <caption><h1>Liste des formateurs</h1></caption>
    <tbody>
    {% for formateur in formateurs %}
    <tr>
        <td><a href="{{ path('show_formateur', {'id':formateur.id}) }}">{{ formateur }}</a></td>
        <td><a class="btn1" href="{{ path('edit_formateur', {'id':formateur.id}) }}">Editer</a></td>
        <td><a class="btn1" href="{{ path('delete_formateur', {'id':formateur.id}) }}">Supprimer</a></td>
    </tr>
    {% endfor %}
    </tbody>    
    </table>
=======
    <h1>Liste des formateurs</h1>
>>>>>>> parent of fb1cde4... Add&Edit&Delete Done

    <p>
    <a class="btn" href="{{ path('add_formateur') }}">Ajouter un formateur</a>
    </p>

    {% for formateur in formateurs %}
    <a href="{{ path('show_formateur', {'id':formateur.id}) }}">{{ formateur }}</a><br> 
    <a href="{{ path('edit_formateur', {'id':formateur.id}) }}">Editer</a><br> 
    <a href="{{ path('delete_formateur', {'id':formateur.id}) }}">Supprimer</a><br> 
    {% endfor %}

{% endblock %}
