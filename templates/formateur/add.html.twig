{% extends 'base.html.twig' %}

{% block title %}Ajouter un formateur{% endblock %}

{% block body %}
    {% if edit %}
     <h1>Editer un formateur</h1>
    {% else %}
     <h1>Ajouter un formateur</h1>
    {% endif %}
 
    {# {{ form_start(formAddFormateur)}}
    {{ form_row(formAddFormateur)}}   
    {{ form_end(formAddFormateur)}} #}
<div class="add">
    {{ form(formAddFormateur)}}
</div>
{% endblock %}